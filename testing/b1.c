/* program 1 of boolean values */
#include <stdio.h>

int main(void) {
    /* initial variables */
    int i = 1, j = 0;
    int k;

    /* (0 + 1 + 1 + 1) = 3 */
    k = (i <= 0) + (j >= 00) + (i >= 0) + (j <= 0);
    printf("%d\n",k);
    return 0;
}
